<h1>List are below</h1>

<% @lists.each do |list| %>
  <p><%= list.name %> <%= link_to "See the list", list_path(list.id), class: "btn btn-warning"%>

<% end %>
<p><%= link_to "Create a new list", new_list_path, class: "btn btn-success"%></p>
